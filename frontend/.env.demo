# Loaded only by `npm run demo` (vite --mode demo). Enables the demo experience:
# the "Load demo data" control and visibility of [Demo] estimates. Absent in
# dev/prod, so demo never shows there.
VITE_DEMO_MODE=true
